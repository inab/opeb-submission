diff -Nru /etc/ssh.template/sshd_config /etc/ssh_chroot/sshd_config
--- /etc/ssh.template/sshd_config	2017-05-02 14:37:36.100000000 +0200
+++ /etc/ssh_chroot/sshd_config	2017-11-29 19:27:25.576000000 +0100
@@ -2,16 +2,16 @@
 # See the sshd_config(5) manpage for details
 
 # What ports, IPs and protocols we listen for
-Port 22
+Port 2222
 # Use these options to restrict which interfaces/protocols sshd will bind to
 #ListenAddress ::
 #ListenAddress 0.0.0.0
 Protocol 2
 # HostKeys for protocol version 2
-HostKey /etc/ssh/ssh_host_rsa_key
-HostKey /etc/ssh/ssh_host_dsa_key
-HostKey /etc/ssh/ssh_host_ecdsa_key
-HostKey /etc/ssh/ssh_host_ed25519_key
+HostKey /etc/ssh_chroot/ssh_host_rsa_key
+HostKey /etc/ssh_chroot/ssh_host_dsa_key
+HostKey /etc/ssh_chroot/ssh_host_ecdsa_key
+HostKey /etc/ssh_chroot/ssh_host_ed25519_key
 #Privilege Separation is turned on for security
 UsePrivilegeSeparation yes
 
@@ -25,7 +25,7 @@
 
 # Authentication:
 LoginGraceTime 120
-PermitRootLogin yes
+PermitRootLogin no
 StrictModes yes
 
 RSAAuthentication yes
@@ -74,8 +74,6 @@
 # Allow client to pass locale environment variables
 AcceptEnv LANG LC_*
 
-Subsystem sftp /usr/lib/openssh/sftp-server
-
 # Set this to 'yes' to enable PAM authentication, account processing,
 # and session processing. If this is enabled, PAM authentication will
 # be allowed through the ChallengeResponseAuthentication and
@@ -86,3 +84,12 @@
 # PAM authentication, then enable this but set PasswordAuthentication
 # and ChallengeResponseAuthentication to 'no'.
 UsePAM yes
+
+PidFile /run/sshd_chroot.pid
+AllowGroups submitters
+Subsystem sftp internal-sftp
+Match Group submitters LocalPort 2222
+	ChrootDirectory /data/submitters/%u
+	X11Forwarding no
+	AllowTcpForwarding no
+	ForceCommand internal-sftp
